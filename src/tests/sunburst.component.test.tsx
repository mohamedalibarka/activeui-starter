import React from 'react';
import { render } from '@testing-library/react';
import '@testing-library/jest-dom/extend-expect';
import { Sunburst } from '../sunburst/Sunburst';

// TODO : continuer

test('Renders plugin', () => {
      
      render(<BaseSunburst queryResult="..." onSelectionChange="..."/>
      );
})
